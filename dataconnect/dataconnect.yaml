specVersion: "v1"
serviceId: "mycollege-web-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mycollege-web-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
